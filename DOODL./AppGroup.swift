import Foundation

enum AppGroup {
    // Requires enabling App Groups for BOTH the app target and the widget extension target.
    // In Xcode: Signing & Capabilities → + Capability → App Groups
    static let identifier = "group.com.anthonyverruijt.doodl"
}

