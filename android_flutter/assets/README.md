Put your Android assets here.

Expected (optional) files:
- `assets/images/logo.png` (recommended): your DOODL. logo used on onboarding + dashboard.

If the logo file is missing, the app falls back to a text logo.
